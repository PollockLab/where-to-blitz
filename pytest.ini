[pytest]
# test_ingest_stats.py reads the live Arbutus bucket and the Biodiversite Quebec tiler, so it
# fails on their downtime rather than on ours. Deselected by default; run it with `-m network`.
markers =
    network: hits third-party endpoints over the internet
addopts = -m "not network"
