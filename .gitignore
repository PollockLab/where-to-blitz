# Python env / caches
.venv/
__pycache__/
*.pyc

# OS
.DS_Store

# Heavy, re-derivable raster inputs (CHELSA/Hansen/Weiss clips) and raw iNat pulls
*.tif
cluster_results/inat_*.csv
*.log

# claude-flow runtime/session state
.claude-flow/

# local embedding cache for offline strategy sweeps (regenerable)
local_cache/

# #87 vector-tile spike: generated grid tiles + tmp (re-derivable via build_vector_tiles.py)
cluster_results/_vtiles_tmp/
density/cells5.pmtiles
density/cells25.pmtiles
density/_gap5_raster.pmtiles
