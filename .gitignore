# Python env / caches
.venv/
__pycache__/
*.pyc

# OS
.DS_Store

# Heavy, re-derivable raster inputs (CHELSA/Hansen/Weiss clips) and raw iNat pulls
*.tif
cluster_results/inat_*.csv
*.log

# Per-tier lattice stacks: the intermediate build_fullgrid_ca.py hands to build_grid_pmtiles.py (#87)
cluster_results/ca/grid_*m/

# claude-flow runtime/session state
.claude-flow/

# local embedding cache for offline strategy sweeps (regenerable)
local_cache/

# Derived tiles/density we keep out of git (built in CI)
grid/
density/
tiles/
cluster_results/ca/*.pmtiles
cluster_results/ca/pmtiles/
